# =============================================================================
# Azure Container Apps - Web Environment Variables
# =============================================================================
# Copy this file and configure for your Azure deployment
# NEXT_PUBLIC_* variables are embedded at BUILD TIME in Next.js
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# IMPORTANT: This must be set at BUILD TIME (docker build --build-arg)
# Use the FQDN of your API Container App (without /api suffix - client adds it)
NEXT_PUBLIC_API_URL=https://api.your-env.azurecontainerapps.io

# =============================================================================
# Build-Time vs Runtime Variables:
# =============================================================================
# NEXT_PUBLIC_* = Embedded at build time (must be passed as build args)
# Other vars    = Available at runtime (can be set in Container Apps config)
#
# For Azure deployment, pass NEXT_PUBLIC_API_URL as a build argument:
#   docker build --build-arg NEXT_PUBLIC_API_URL=https://api.example.com
# =============================================================================
